services:
  shop-bot:
    image: telegram-shop:latest
    env_file: bot.env
    environment:
      DATABASE_PATH: /app/data/data.db
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
